
void debug_print_detector(Detector* d);

void debug_print_prob_dist_f(
        float dist[const],
        const size_t num_bins,
        const char* name,
        const char* spec);

void debug_print_tooth_map(
        uint8_t map[const],
        const size_t num_bins,
        const char* name,
        const char* spec);
